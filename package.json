{
    "name": "sp-sortable-data-table-monorepo",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "lerna run build",
        "test": "lerna run test"
    },
    "devDependencies": {
        "@semantic-release/github": "^11.0.1",
        "@semantic-release/npm": "^12.0.1",
        "lerna": "^4.0.0",
        "semantic-release": "^24.2.3",
        "typescript": "^4.0.0"
    }
}
